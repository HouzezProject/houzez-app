/* eslint-disable prettier/prettier */
import styled from "@emotion/styled";
import { Divider } from "@mui/material"

const HzDivider = styled(Divider)({
    width: "100%",
});

export default HzDivider;
