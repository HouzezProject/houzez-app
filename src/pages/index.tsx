import type { NextPage } from 'next';
import { DialogTitle } from '@mui/material';

const Home: NextPage = () => {
  return <DialogTitle>This is where we start to develop our frontend</DialogTitle>;
};

export default Home;
